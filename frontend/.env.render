# =============================================================================
# RENDER DEPLOYMENT - FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy each line below and add to Render Dashboard:
# Dashboard → video-call-frontend → Environment → Add Environment Variable
#
# IMPORTANT: Add these ONE BY ONE in Render Dashboard
# DO NOT upload this file - Render doesn't read .env files!
# After adding, frontend will REBUILD (takes 2-3 minutes)
# =============================================================================

REACT_APP_API_URL=https://video-call-backend-nb87.onrender.com
REACT_APP_SOCKET_URL=https://video-call-backend-nb87.onrender.com
REACT_APP_SOCKET_PATH=/socket.io
REACT_APP_SOCKET_RECONNECTION=true
REACT_APP_SOCKET_RECONNECTION_DELAY=1000
REACT_APP_SOCKET_RECONNECTION_ATTEMPTS=10
REACT_APP_WEBRTC_AUDIO_ENABLED=true
REACT_APP_WEBRTC_VIDEO_ENABLED=true
REACT_APP_ENABLE_CHAT=true
REACT_APP_ENABLE_WHITEBOARD=true
REACT_APP_ENABLE_SCREEN_SHARE=true
REACT_APP_ENABLE_POLLS=true
REACT_APP_ENABLE_ATTENDANCE=true

# =============================================================================
# HOW TO ADD THESE TO RENDER:
# =============================================================================
# 1. Go to: https://dashboard.render.com
# 2. Click: "video-call-frontend" service
# 3. Click: "Environment" tab (left sidebar)
# 4. For each line above (except comments):
#    - Click "Add Environment Variable"
#    - Key: Copy the part BEFORE the = sign (e.g., REACT_APP_API_URL)
#    - Value: Copy the part AFTER the = sign (e.g., https://video-call-backend-nb87.onrender.com)
# 5. Click "Save Changes" at bottom
# 6. Wait 2-3 minutes for frontend to REBUILD
#
# MOST CRITICAL VARIABLES (add these first if short on time):
# - REACT_APP_API_URL (must match backend URL exactly!)
# - REACT_APP_SOCKET_URL (must match backend URL exactly!)
# =============================================================================
