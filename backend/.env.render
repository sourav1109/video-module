# =============================================================================
# RENDER DEPLOYMENT - BACKEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy each line below and add to Render Dashboard:
# Dashboard → video-call-backend → Environment → Add Environment Variable
#
# IMPORTANT: Add these ONE BY ONE in Render Dashboard
# DO NOT upload this file - Render doesn't read .env files!
# =============================================================================

NODE_ENV=production
PORT=5000
DATABASE_URL=postgresql://neondb_owner:npg_Md8Lk6fromCY@ep-old-shadow-adhkxq37-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
JWT_SECRET=video-call-super-secret-jwt-key-change-in-production-12345
JWT_EXPIRE=7d
USE_REDIS=false
CORS_ORIGIN=https://video-call-frontend-jwku.onrender.com
MEDIASOUP_WORKER_COUNT=8
TRUST_PROXY=1
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# =============================================================================
# HOW TO ADD THESE TO RENDER:
# =============================================================================
# 1. Go to: https://dashboard.render.com
# 2. Click: "video-call-backend" service
# 3. Click: "Environment" tab (left sidebar)
# 4. For each line above (except comments):
#    - Click "Add Environment Variable"
#    - Key: Copy the part BEFORE the = sign (e.g., NODE_ENV)
#    - Value: Copy the part AFTER the = sign (e.g., production)
# 5. Click "Save Changes" at bottom
# 6. Wait 30-60 seconds for backend to restart
#
# MOST CRITICAL VARIABLES (add these first if short on time):
# - JWT_SECRET (fixes Socket.IO authentication!)
# - USE_REDIS (must be false for free tier)
# - DATABASE_URL (your Neon PostgreSQL connection)
# =============================================================================
